'use babel';

import ProvisioningCheckerView from '../lib/provisioning-checker-view';

describe('ProvisioningCheckerView', () => {
  it('has one valid test', () => {
    // expect('life').toBe('easy');
  });
});
